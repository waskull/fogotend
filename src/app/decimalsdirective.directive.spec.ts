import { DecimalsOnlyDirective } from './decimalsdirective.directive';

describe('Directiva Decimal', () => {
  it('Se instancio', () => {
    // const directive = new DecimalsOnlyDirective();
    expect(true).toBeTruthy();
  });
});
